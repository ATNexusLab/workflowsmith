# Compiler

This directory contains contracts for compiling canonical WorkflowSmith source into harness-specific distributions.

The 0.0.0 milestone does not implement a compiler. It defines where compiler contracts live and how future harness mappings are governed.
