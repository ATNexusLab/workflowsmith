{
  "version": 1,
  "hooks": {
    "preToolUse": [
      {
        "type": "command",
        "bash": "INPUT=$(cat); if command -v jq >/dev/null 2>&1; then CMD=$(echo \"$INPUT\" | jq -r '.toolArgs.command // \"\"'); CWD=$(echo \"$INPUT\" | jq -r '.cwd // \".\"'); elif command -v python3 >/dev/null 2>&1; then CMD=$(echo \"$INPUT\" | python3 -c 'import json,sys; d=json.load(sys.stdin); print(d.get(\"toolArgs\",{}).get(\"command\",\"\"))' 2>/dev/null || echo ''); CWD=$(echo \"$INPUT\" | python3 -c 'import json,sys; d=json.load(sys.stdin); print(d.get(\"cwd\",\".\"))' 2>/dev/null || echo '.'); else echo '{}'; exit 0; fi; if echo \"$CMD\" | grep -qiE 'rm[[:space:]]+-rf[[:space:]]+(/|~|\\$HOME|\\.\\.)'; then echo '{\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Blocked: rm -rf with broad or absolute path is not allowed.\"}'; exit 0; fi; if echo \"$CMD\" | grep -qiE '(DROP[[:space:]]+TABLE|TRUNCATE[[:space:]]+TABLE|DELETE[[:space:]]+FROM[[:space:]]+[a-zA-Z_]+[[:space:]]*(;|$))' && ! echo \"$CMD\" | grep -qiE 'WHERE'; then echo '{\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Blocked: destructive SQL (DROP/TRUNCATE/DELETE) without WHERE clause is not allowed.\"}'; exit 0; fi; BRANCH=$(git -C \"$CWD\" rev-parse --abbrev-ref HEAD 2>/dev/null || echo ''); if echo \"$CMD\" | grep -qiE 'push[[:space:]]+(--force|-f)' && echo \"$BRANCH\" | grep -qiE '^(main|master|production|prod|release)$'; then echo '{\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Blocked: force-push to protected branch is not allowed.\"}'; exit 0; fi; echo '{}'; exit 0",
        "timeoutSec": 5
      }
    ]
  }
}
